#include "Ono.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = $(inherited) COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/Headers/Private" "${PODS_ROOT}/Headers/Private/Ono" "${PODS_ROOT}/Headers/Public" ${ONO_HEADER_SEARCH_PATHS}
OTHER_LDFLAGS = ${ONO_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}
SKIP_INSTALL = YES